# Android Native Shell

The Android shell is a Kotlin/Compose Scheduler surface backed by mocked
contract-shaped schedule state.

It displays:

- tenant and schedule identity
- manager mode
- worker mode
- mocked shifts

No Flutter code is copied into this repo.
