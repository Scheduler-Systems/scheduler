# iOS Native Shell

The iOS shell is a SwiftUI Scheduler surface backed by mocked contract-shaped
schedule state.

It displays tenant and schedule identity, manager mode, worker mode, and mocked
shifts. No Flutter code is copied into this repo.
