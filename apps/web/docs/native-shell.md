# Web Native Shell

The web shell is a native browser Scheduler surface backed by mocked
contract-shaped schedule state.

Run:

```sh
make test
make dev
```

It displays tenant and schedule identity, manager mode, worker mode, and mocked
shifts.
